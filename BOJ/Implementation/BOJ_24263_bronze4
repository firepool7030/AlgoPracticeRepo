# BOJ_24263_bronze4
import sys
input = sys.stdin.readline
output = sys.stdout.write

def men_of_passion(arr, n):
    sum_of_num = 0
    for i in range(1, n + 1):
        sum_of_num += arr[i]
    return sum_of_num

n = int(input())
output(f"{n}\n{1}")
